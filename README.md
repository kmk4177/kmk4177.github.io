# realtime-balls

## Local
npm install
npm start
open http://localhost:3000

## Deploy
- Uses process.env.PORT
- Optional: ALLOWED_ORIGINS="https://username.github.io,https://yourdomain.com"
- Health: /healthz
